package Assignment_2;

public abstract class Truck {

	/* 
	 * Any truck that is created is put into an
	 * Array of an Array List of Type Truck and 
	 * put into the file vice versa with the initial 
	 * check of the file putting the file information 
	 * into the same array
	 * 
	 * Somehow done with manifest
	 */
}
